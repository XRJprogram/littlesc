# littlesc
littlesc is an intelligent assistant for Scratch 3.0. Users describe logic in natural language in the editor sidebar; a built-in LLM gateway generates goboscript code, which a pure Node.js compiler converts into Scratch VM blocks and merges into the project. It also offers diff comparison and visual block localization.
